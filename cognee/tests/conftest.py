"""Global test configuration."""

# Intentionally left minimal. Optional binary dependencies such as ``numpy``
# and ``tokenizers`` are handled within individual test modules using
# ``pytest.importorskip`` when required.
