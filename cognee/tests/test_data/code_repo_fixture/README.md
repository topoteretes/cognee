# inventory-fixture

A tiny, pinned Python project used by `cognee/tests/test_code_graph_e2e.py`.

Do not change the symbols or call chain in `inventory/` without updating the
known-answer assertions in that test.
