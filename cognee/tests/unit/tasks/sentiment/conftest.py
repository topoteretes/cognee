"""Sentiment test configuration."""

# This file intentionally avoids globally stubbing optional binary dependencies.
# Individual tests should handle optional imports explicitly (e.g., with
# ``pytest.importorskip``) to ensure regressions are not masked.
