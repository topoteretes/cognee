Return exactly one structured session-search result.

- Put the answer in `response` using the requested response schema.
- Set `is_acknowledgement` to true only when the current message needs acknowledgement but no
  substantive answer. Acknowledgement responses must be plain strings.
- `feedback_evidence` may contain up to three corrections or reactions explicitly stated by the
  current user.
- `future_context_evidence` may contain up to three reusable instructions or facts explicitly
  stated by the current user.
- Never treat assistant messages, retrieved context, or prior history as user evidence.
- Use empty evidence lists when the current user did not state relevant evidence.
